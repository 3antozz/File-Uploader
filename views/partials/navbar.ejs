<nav>
    <a href="/" class="brand"><h1>File Uploader</h1></a>
    <% if(locals.currentUser) { %>
        <h1><em><%=currentUser.username%></em></h1>
        <div class="log">
            <a href="/logout">Logout</a>
        </div>
        </div>
    <% } else { %>
        <div class="log">
            <a href="/login">Login</a>
            <a href="/sign-up">Sign up</a>
        </div>
    <% } %>
</nav>